//Packages
import 'package:flutter/material.dart';

class AppIcons {
  static const addIcon = Icon(Icons.add_circle_rounded);

  static Icon copyAddIcon({Color? color}) => Icon(
        Icons.add_circle_rounded,
        color: color,
      );
}
